"""
Rendering functions.

All apply antialiasing to avoid the formation of `moire patterns` by default.
"""

def render_weights(shape, pins, weights):
    pass
    return render

def render_steps(shape, pins, steps):
    pass
    return render
